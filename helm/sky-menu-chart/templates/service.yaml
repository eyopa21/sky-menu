apiVersion: v1
kind: Service
metadata:
  name: {{ include "sky-menu-chart.fullname" . }}
  labels:
    {{- include "sky-menu-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sky-menu-chart.selectorLabels" . | nindent 4 }}
