apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sky-menu-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sky-menu-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sky-menu-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
